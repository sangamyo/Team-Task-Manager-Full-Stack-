# Nixpacks configuration for Next.js frontend deployment
# This ensures Node.js 20.x and proper Next.js build steps

[languages]
nodes = ["20.17.1"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
